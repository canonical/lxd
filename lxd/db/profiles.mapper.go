//go:build linux && cgo && !agent
// +build linux,cgo,!agent

package db

// The code below was generated by lxd-generate - DO NOT EDIT!

import (
	"fmt"
	"github.com/lxc/lxd/lxd/db/cluster"
	"github.com/lxc/lxd/lxd/db/query"
	"github.com/lxc/lxd/shared/api"
	"github.com/pkg/errors"
	"strings"
)

var _ = api.ServerEnvironment{}

var profileNames = cluster.RegisterStmt(`
SELECT projects.name AS project, profiles.name
  FROM profiles JOIN projects ON profiles.project_id = projects.id
  ORDER BY projects.id, profiles.name
`)

var profileNamesByProject = cluster.RegisterStmt(`
SELECT projects.name AS project, profiles.name
  FROM profiles JOIN projects ON profiles.project_id = projects.id
  WHERE project = ? ORDER BY projects.id, profiles.name
`)

var profileNamesByProjectAndName = cluster.RegisterStmt(`
SELECT projects.name AS project, profiles.name
  FROM profiles JOIN projects ON profiles.project_id = projects.id
  WHERE project = ? AND profiles.name = ? ORDER BY projects.id, profiles.name
`)

var profileObjects = cluster.RegisterStmt(`
SELECT profiles.id, projects.name AS project, profiles.name, coalesce(profiles.description, '')
  FROM profiles JOIN projects ON profiles.project_id = projects.id
  ORDER BY projects.id, profiles.name
`)

var profileObjectsByProject = cluster.RegisterStmt(`
SELECT profiles.id, projects.name AS project, profiles.name, coalesce(profiles.description, '')
  FROM profiles JOIN projects ON profiles.project_id = projects.id
  WHERE project = ? ORDER BY projects.id, profiles.name
`)

var profileObjectsByProjectAndName = cluster.RegisterStmt(`
SELECT profiles.id, projects.name AS project, profiles.name, coalesce(profiles.description, '')
  FROM profiles JOIN projects ON profiles.project_id = projects.id
  WHERE project = ? AND profiles.name = ? ORDER BY projects.id, profiles.name
`)

var profileConfigRef = cluster.RegisterStmt(`
SELECT project, name, key, value FROM profiles_config_ref ORDER BY project, name
`)

var profileConfigRefByProject = cluster.RegisterStmt(`
SELECT project, name, key, value FROM profiles_config_ref WHERE project = ? ORDER BY project, name
`)

var profileConfigRefByProjectAndName = cluster.RegisterStmt(`
SELECT project, name, key, value FROM profiles_config_ref WHERE project = ? AND name = ? ORDER BY project, name
`)

var profileDevicesRef = cluster.RegisterStmt(`
SELECT project, name, device, type, key, value FROM profiles_devices_ref ORDER BY project, name
`)

var profileDevicesRefByProject = cluster.RegisterStmt(`
SELECT project, name, device, type, key, value FROM profiles_devices_ref WHERE project = ? ORDER BY project, name
`)

var profileDevicesRefByProjectAndName = cluster.RegisterStmt(`
SELECT project, name, device, type, key, value FROM profiles_devices_ref WHERE project = ? AND name = ? ORDER BY project, name
`)

var profileUsedByRef = cluster.RegisterStmt(`
SELECT project, name, value FROM profiles_used_by_ref ORDER BY project, name
`)

var profileUsedByRefByProject = cluster.RegisterStmt(`
SELECT project, name, value FROM profiles_used_by_ref WHERE project = ? ORDER BY project, name
`)

var profileUsedByRefByProjectAndName = cluster.RegisterStmt(`
SELECT project, name, value FROM profiles_used_by_ref WHERE project = ? AND name = ? ORDER BY project, name
`)

var profileID = cluster.RegisterStmt(`
SELECT profiles.id FROM profiles JOIN projects ON profiles.project_id = projects.id
  WHERE projects.name = ? AND profiles.name = ?
`)

var profileCreate = cluster.RegisterStmt(`
INSERT INTO profiles (project_id, name, description)
  VALUES ((SELECT projects.id FROM projects WHERE projects.name = ?), ?, ?)
`)

var profileCreateConfigRef = cluster.RegisterStmt(`
INSERT INTO profiles_config (profile_id, key, value)
  VALUES (?, ?, ?)
`)

var profileCreateDevicesRef = cluster.RegisterStmt(`
INSERT INTO profiles_devices (profile_id, name, type)
  VALUES (?, ?, ?)
`)
var profileCreateDevicesConfigRef = cluster.RegisterStmt(`
INSERT INTO profiles_devices_config (profile_device_id, key, value)
  VALUES (?, ?, ?)
`)

var profileRename = cluster.RegisterStmt(`
UPDATE profiles SET name = ? WHERE project_id = (SELECT projects.id FROM projects WHERE projects.name = ?) AND name = ?
`)

var profileDeleteByProjectAndName = cluster.RegisterStmt(`
DELETE FROM profiles WHERE project_id = (SELECT projects.id FROM projects WHERE projects.name = ?) AND name = ?
`)

var profileDeleteConfigRef = cluster.RegisterStmt(`
DELETE FROM profiles_config WHERE profile_id = ?
`)

var profileDeleteDevicesRef = cluster.RegisterStmt(`
DELETE FROM profiles_devices WHERE profile_id = ?
`)

var profileUpdate = cluster.RegisterStmt(`
UPDATE profiles
  SET project_id = (SELECT id FROM projects WHERE name = ?), name = ?, description = ?
 WHERE id = ?
`)

// GetProfileURIs returns all available profile URIs.
// generator: profile URIs
func (c *ClusterTx) GetProfileURIs(filter ProfileFilter) ([]string, error) {
	stmt := c.stmt(profileNames)
	args := []interface{}{}
	stmtStr := cluster.GetRegisteredStmt(profileNames)
	if filter.Description != "" {
		args = append(args, filter.Description)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "description", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	code := cluster.EntityTypes["profile"]
	formatter := cluster.EntityFormatURIs[code]

	return query.SelectURIs(stmt, formatter, args...)
}

// GetProfileURIsByProject returns all available profile URIs.
// generator: profile URIs-by-Project
func (c *ClusterTx) GetProfileURIsByProject(project string, filter ProfileFilter) ([]string, error) {
	stmt := c.stmt(profileNamesByProject)
	args := []interface{}{
		project,
	}
	stmtStr := cluster.GetRegisteredStmt(profileNamesByProject)
	if filter.Description != "" {
		args = append(args, filter.Description)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "description", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	code := cluster.EntityTypes["profile"]
	formatter := cluster.EntityFormatURIs[code]

	return query.SelectURIs(stmt, formatter, args...)
}

// GetProfileURIsByProjectAndName returns all available profile URIs.
// generator: profile URIs-by-Project-and-Name
func (c *ClusterTx) GetProfileURIsByProjectAndName(project string, name string, filter ProfileFilter) ([]string, error) {
	stmt := c.stmt(profileNamesByProjectAndName)
	args := []interface{}{
		project,
		name,
	}
	stmtStr := cluster.GetRegisteredStmt(profileNamesByProjectAndName)
	if filter.Description != "" {
		args = append(args, filter.Description)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "description", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	code := cluster.EntityTypes["profile"]
	formatter := cluster.EntityFormatURIs[code]

	return query.SelectURIs(stmt, formatter, args...)
}

// GetProfiles returns all available profiles.
// generator: profile List
func (c *ClusterTx) GetProfiles(filter ProfileFilter) ([]Profile, error) {
	// Result slice.
	objects := make([]Profile, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	stmt := c.stmt(profileObjects)
	args := []interface{}{}
	stmtStr := cluster.GetRegisteredStmt(profileObjects)
	if filter.Description != "" {
		args = append(args, filter.Description)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "description", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, Profile{})
		return []interface{}{
			&objects[i].ID,
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Description,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch profiles")
	}

	// Fill field Config.
	configObjects, err := c.ProfileConfigRef()
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field Config")
	}

	for i := range objects {
		_, ok0 := configObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string]map[string]string{}
			configObjects[objects[i].Project] = subIndex
		}

		value := configObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = map[string]string{}
		}
		objects[i].Config = value
	}

	// Fill field Devices.
	devicesObjects, err := c.ProfileDevicesRef()
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field Devices")
	}

	for i := range objects {
		_, ok0 := devicesObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string]map[string]map[string]string{}
			devicesObjects[objects[i].Project] = subIndex
		}

		value := devicesObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = map[string]map[string]string{}
		}
		objects[i].Devices = value
	}

	// Fill field UsedBy.
	usedByObjects, err := c.ProfileUsedByRef()
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field UsedBy")
	}

	for i := range objects {
		_, ok0 := usedByObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string][]string{}
			usedByObjects[objects[i].Project] = subIndex
		}

		value := usedByObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = []string{}
		}
		for j := range value {
			if len(value[j]) > 12 && value[j][len(value[j])-12:] == "&target=none" {
				value[j] = value[j][0 : len(value[j])-12]
			}
			if len(value[j]) > 16 && value[j][len(value[j])-16:] == "?project=default" {
				value[j] = value[j][0 : len(value[j])-16]
			}
		}
		objects[i].UsedBy = value
	}

	return objects, nil
}

// GetProfilesByProject returns all available profiles.
// generator: profile List-by-Project
func (c *ClusterTx) GetProfilesByProject(project string, filter ProfileFilter) ([]Profile, error) {
	// Result slice.
	objects := make([]Profile, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	stmt := c.stmt(profileObjectsByProject)
	args := []interface{}{
		project,
	}
	stmtStr := cluster.GetRegisteredStmt(profileObjectsByProject)
	if filter.Description != "" {
		args = append(args, filter.Description)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "description", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, Profile{})
		return []interface{}{
			&objects[i].ID,
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Description,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch profiles")
	}

	// Fill field Config.
	configObjects, err := c.ProfileConfigRefByProject(project)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field Config")
	}

	for i := range objects {
		_, ok0 := configObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string]map[string]string{}
			configObjects[objects[i].Project] = subIndex
		}

		value := configObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = map[string]string{}
		}
		objects[i].Config = value
	}

	// Fill field Devices.
	devicesObjects, err := c.ProfileDevicesRefByProject(project)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field Devices")
	}

	for i := range objects {
		_, ok0 := devicesObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string]map[string]map[string]string{}
			devicesObjects[objects[i].Project] = subIndex
		}

		value := devicesObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = map[string]map[string]string{}
		}
		objects[i].Devices = value
	}

	// Fill field UsedBy.
	usedByObjects, err := c.ProfileUsedByRefByProject(project)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field UsedBy")
	}

	for i := range objects {
		_, ok0 := usedByObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string][]string{}
			usedByObjects[objects[i].Project] = subIndex
		}

		value := usedByObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = []string{}
		}
		for j := range value {
			if len(value[j]) > 12 && value[j][len(value[j])-12:] == "&target=none" {
				value[j] = value[j][0 : len(value[j])-12]
			}
			if len(value[j]) > 16 && value[j][len(value[j])-16:] == "?project=default" {
				value[j] = value[j][0 : len(value[j])-16]
			}
		}
		objects[i].UsedBy = value
	}

	return objects, nil
}

// GetProfilesByProjectAndName returns all available profiles.
// generator: profile List-by-Project-and-Name
func (c *ClusterTx) GetProfilesByProjectAndName(project string, name string, filter ProfileFilter) ([]Profile, error) {
	// Result slice.
	objects := make([]Profile, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	stmt := c.stmt(profileObjectsByProjectAndName)
	args := []interface{}{
		project,
		name,
	}
	stmtStr := cluster.GetRegisteredStmt(profileObjectsByProjectAndName)
	if filter.Description != "" {
		args = append(args, filter.Description)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "description", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, Profile{})
		return []interface{}{
			&objects[i].ID,
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Description,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch profiles")
	}

	// Fill field Config.
	configObjects, err := c.ProfileConfigRefByProjectAndName(project, name)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field Config")
	}

	for i := range objects {
		_, ok0 := configObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string]map[string]string{}
			configObjects[objects[i].Project] = subIndex
		}

		value := configObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = map[string]string{}
		}
		objects[i].Config = value
	}

	// Fill field Devices.
	devicesObjects, err := c.ProfileDevicesRefByProjectAndName(project, name)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field Devices")
	}

	for i := range objects {
		_, ok0 := devicesObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string]map[string]map[string]string{}
			devicesObjects[objects[i].Project] = subIndex
		}

		value := devicesObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = map[string]map[string]string{}
		}
		objects[i].Devices = value
	}

	// Fill field UsedBy.
	usedByObjects, err := c.ProfileUsedByRefByProjectAndName(project, name)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch field UsedBy")
	}

	for i := range objects {
		_, ok0 := usedByObjects[objects[i].Project]
		if !ok0 {
			subIndex := map[string][]string{}
			usedByObjects[objects[i].Project] = subIndex
		}

		value := usedByObjects[objects[i].Project][objects[i].Name]
		if value == nil {
			value = []string{}
		}
		for j := range value {
			if len(value[j]) > 12 && value[j][len(value[j])-12:] == "&target=none" {
				value[j] = value[j][0 : len(value[j])-12]
			}
			if len(value[j]) > 16 && value[j][len(value[j])-16:] == "?project=default" {
				value[j] = value[j][0 : len(value[j])-16]
			}
		}
		objects[i].UsedBy = value
	}

	return objects, nil
}

// GetProfileByProjectAndName returns the profile with the given key.
// generator: profile Get-by-Project-and-Name
func (c *ClusterTx) GetProfileByProjectAndName(project string, name string, filter ProfileFilter) (*Profile, error) {
	objects, err := c.GetProfilesByProjectAndName(project, name, filter)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch Profile")
	}

	switch len(objects) {
	case 0:
		return nil, ErrNoSuchObject
	case 1:
		return &objects[0], nil
	default:
		return nil, fmt.Errorf("More than one profile matches")
	}
}

// ProfileExists checks if a profile with the given key exists.
// generator: profile Exists
func (c *ClusterTx) ProfileExists(project string, name string) (bool, error) {
	_, err := c.GetProfileID(project, name)
	if err != nil {
		if err == ErrNoSuchObject {
			return false, nil
		}
		return false, err
	}

	return true, nil
}

// GetProfileID return the ID of the profile with the given key.
// generator: profile ID
func (c *ClusterTx) GetProfileID(project string, name string) (int64, error) {
	stmt := c.stmt(profileID)
	rows, err := stmt.Query(project, name)
	if err != nil {
		return -1, errors.Wrap(err, "Failed to get profile ID")
	}
	defer rows.Close()

	// Ensure we read one and only one row.
	if !rows.Next() {
		return -1, ErrNoSuchObject
	}
	var id int64
	err = rows.Scan(&id)
	if err != nil {
		return -1, errors.Wrap(err, "Failed to scan ID")
	}
	if rows.Next() {
		return -1, fmt.Errorf("More than one row returned")
	}
	err = rows.Err()
	if err != nil {
		return -1, errors.Wrap(err, "Result set failure")
	}

	return id, nil
}

// ProfileConfigRef returns entities used by profiles.
// generator: profile ConfigRef
func (c *ClusterTx) ProfileConfigRef() (map[string]map[string]map[string]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Key     string
		Value   string
	}, 0)

	stmt := c.stmt(profileConfigRef)
	args := []interface{}{}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Key     string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Key,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch  ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string]map[string]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string]map[string]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = map[string]string{}
		}

		index[object.Project][object.Name] = item
		item[object.Key] = object.Value
	}

	return index, nil
}

// ProfileConfigRefByProject returns entities used by profiles.
// generator: profile ConfigRef-by-Project
func (c *ClusterTx) ProfileConfigRefByProject(project string) (map[string]map[string]map[string]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Key     string
		Value   string
	}, 0)

	stmt := c.stmt(profileConfigRefByProject)
	args := []interface{}{
		project,
	}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Key     string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Key,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch  ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string]map[string]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string]map[string]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = map[string]string{}
		}

		index[object.Project][object.Name] = item
		item[object.Key] = object.Value
	}

	return index, nil
}

// ProfileConfigRefByProjectAndName returns entities used by profiles.
// generator: profile ConfigRef-by-Project-and-Name
func (c *ClusterTx) ProfileConfigRefByProjectAndName(project string, name string) (map[string]map[string]map[string]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Key     string
		Value   string
	}, 0)

	stmt := c.stmt(profileConfigRefByProjectAndName)
	args := []interface{}{
		project,
		name,
	}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Key     string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Key,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch  ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string]map[string]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string]map[string]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = map[string]string{}
		}

		index[object.Project][object.Name] = item
		item[object.Key] = object.Value
	}

	return index, nil
}

// ProfileDevicesRef returns entities used by profiles.
// generator: profile DevicesRef
func (c *ClusterTx) ProfileDevicesRef() (map[string]map[string]map[string]map[string]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Device  string
		Type    int
		Key     string
		Value   string
	}, 0)

	stmt := c.stmt(profileDevicesRef)
	args := []interface{}{}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Device  string
			Type    int
			Key     string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Device,
			&objects[i].Type,
			&objects[i].Key,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch  ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string]map[string]map[string]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string]map[string]map[string]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = map[string]map[string]string{}
		}

		index[object.Project][object.Name] = item
		config, ok := item[object.Device]
		if !ok {
			// First time we see this device, let's int the config
			// and add the type.
			deviceType, err := deviceTypeToString(object.Type)
			if err != nil {
				return nil, errors.Wrapf(
					err, "unexpected device type code '%d'", object.Type)
			}
			config = map[string]string{}
			config["type"] = deviceType
			item[object.Device] = config
		}
		if object.Key != "" {
			config[object.Key] = object.Value
		}
	}

	return index, nil
}

// ProfileDevicesRefByProject returns entities used by profiles.
// generator: profile DevicesRef-by-Project
func (c *ClusterTx) ProfileDevicesRefByProject(project string) (map[string]map[string]map[string]map[string]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Device  string
		Type    int
		Key     string
		Value   string
	}, 0)

	stmt := c.stmt(profileDevicesRefByProject)
	args := []interface{}{
		project,
	}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Device  string
			Type    int
			Key     string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Device,
			&objects[i].Type,
			&objects[i].Key,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch  ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string]map[string]map[string]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string]map[string]map[string]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = map[string]map[string]string{}
		}

		index[object.Project][object.Name] = item
		config, ok := item[object.Device]
		if !ok {
			// First time we see this device, let's int the config
			// and add the type.
			deviceType, err := deviceTypeToString(object.Type)
			if err != nil {
				return nil, errors.Wrapf(
					err, "unexpected device type code '%d'", object.Type)
			}
			config = map[string]string{}
			config["type"] = deviceType
			item[object.Device] = config
		}
		if object.Key != "" {
			config[object.Key] = object.Value
		}
	}

	return index, nil
}

// ProfileDevicesRefByProjectAndName returns entities used by profiles.
// generator: profile DevicesRef-by-Project-and-Name
func (c *ClusterTx) ProfileDevicesRefByProjectAndName(project string, name string) (map[string]map[string]map[string]map[string]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Device  string
		Type    int
		Key     string
		Value   string
	}, 0)

	stmt := c.stmt(profileDevicesRefByProjectAndName)
	args := []interface{}{
		project,
		name,
	}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Device  string
			Type    int
			Key     string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Device,
			&objects[i].Type,
			&objects[i].Key,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch  ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string]map[string]map[string]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string]map[string]map[string]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = map[string]map[string]string{}
		}

		index[object.Project][object.Name] = item
		config, ok := item[object.Device]
		if !ok {
			// First time we see this device, let's int the config
			// and add the type.
			deviceType, err := deviceTypeToString(object.Type)
			if err != nil {
				return nil, errors.Wrapf(
					err, "unexpected device type code '%d'", object.Type)
			}
			config = map[string]string{}
			config["type"] = deviceType
			item[object.Device] = config
		}
		if object.Key != "" {
			config[object.Key] = object.Value
		}
	}

	return index, nil
}

// ProfileUsedByRef returns entities used by profiles.
// generator: profile UsedByRef
func (c *ClusterTx) ProfileUsedByRef() (map[string]map[string][]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Value   string
	}, 0)

	stmt := c.stmt(profileUsedByRef)
	args := []interface{}{}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch string ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string][]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string][]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = []string{}
		}

		index[object.Project][object.Name] = append(item, object.Value)
	}

	return index, nil
}

// ProfileUsedByRefByProject returns entities used by profiles.
// generator: profile UsedByRef-by-Project
func (c *ClusterTx) ProfileUsedByRefByProject(project string) (map[string]map[string][]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Value   string
	}, 0)

	stmt := c.stmt(profileUsedByRefByProject)
	args := []interface{}{
		project,
	}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch string ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string][]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string][]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = []string{}
		}

		index[object.Project][object.Name] = append(item, object.Value)
	}

	return index, nil
}

// ProfileUsedByRefByProjectAndName returns entities used by profiles.
// generator: profile UsedByRef-by-Project-and-Name
func (c *ClusterTx) ProfileUsedByRefByProjectAndName(project string, name string) (map[string]map[string][]string, error) {
	// Result slice.
	objects := make([]struct {
		Project string
		Name    string
		Value   string
	}, 0)

	stmt := c.stmt(profileUsedByRefByProjectAndName)
	args := []interface{}{
		project,
		name,
	}
	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, struct {
			Project string
			Name    string
			Value   string
		}{})
		return []interface{}{
			&objects[i].Project,
			&objects[i].Name,
			&objects[i].Value,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch string ref for profiles")
	}

	// Build index by primary name.
	index := map[string]map[string][]string{}

	for _, object := range objects {
		_, ok0 := index[object.Project]
		if !ok0 {
			subIndex := map[string][]string{}
			index[object.Project] = subIndex
		}

		item, ok := index[object.Project][object.Name]
		if !ok {
			item = []string{}
		}

		index[object.Project][object.Name] = append(item, object.Value)
	}

	return index, nil
}

// CreateProfile adds a new profile to the database.
// generator: profile Create
func (c *ClusterTx) CreateProfile(object Profile) (int64, error) {
	// Check if a profile with the same key exists.
	exists, err := c.ProfileExists(object.Project, object.Name)
	if err != nil {
		return -1, errors.Wrap(err, "Failed to check for duplicates")
	}
	if exists {
		return -1, fmt.Errorf("This profile already exists")
	}

	args := make([]interface{}, 3)

	// Populate the statement arguments.
	args[0] = object.Project
	args[1] = object.Name
	args[2] = object.Description

	// Prepared statement to use.
	stmt := c.stmt(profileCreate)

	// Execute the statement.
	result, err := stmt.Exec(args...)
	if err != nil {
		return -1, errors.Wrap(err, "Failed to create profile")
	}

	id, err := result.LastInsertId()
	if err != nil {
		return -1, errors.Wrap(err, "Failed to fetch profile ID")
	}

	// Insert config reference.
	stmt = c.stmt(profileCreateConfigRef)
	for key, value := range object.Config {
		_, err := stmt.Exec(id, key, value)
		if err != nil {
			return -1, errors.Wrap(err, "Insert config for profile")
		}
	}

	// Insert devices reference.
	for name, config := range object.Devices {
		typ, ok := config["type"]
		if !ok {
			return -1, fmt.Errorf("No type for device %s", name)
		}
		typCode, err := deviceTypeToInt(typ)
		if err != nil {
			return -1, errors.Wrapf(err, "Device type code for %s", typ)
		}
		stmt = c.stmt(profileCreateDevicesRef)
		result, err := stmt.Exec(id, name, typCode)
		if err != nil {
			return -1, errors.Wrapf(err, "Insert device %s", name)
		}
		deviceID, err := result.LastInsertId()
		if err != nil {
			return -1, errors.Wrap(err, "Failed to fetch device ID")
		}
		stmt = c.stmt(profileCreateDevicesConfigRef)
		for key, value := range config {
			_, err := stmt.Exec(deviceID, key, value)
			if err != nil {
				return -1, errors.Wrap(err, "Insert config for profile")
			}
		}
	}

	return id, nil
}

// RenameProfile renames the profile matching the given key parameters.
// generator: profile Rename
func (c *ClusterTx) RenameProfile(project string, name string, to string) error {
	stmt := c.stmt(profileRename)
	result, err := stmt.Exec(to, project, name)
	if err != nil {
		return errors.Wrap(err, "Rename profile")
	}

	n, err := result.RowsAffected()
	if err != nil {
		return errors.Wrap(err, "Fetch affected rows")
	}
	if n != 1 {
		return fmt.Errorf("Query affected %d rows instead of 1", n)
	}
	return nil
}

// DeleteProfileByProjectAndName deletes the profile matching the given key parameters.
// generator: profile DeleteOne-by-Project-and-Name
func (c *ClusterTx) DeleteProfileByProjectAndName(project string, name string, filter ProfileFilter) error {
	stmt := c.stmt(profileDeleteByProjectAndName)
	args := []interface{}{
		project,
		name,
	}
	stmtStr := cluster.GetRegisteredStmt(profileDeleteByProjectAndName)
	if filter.Description != "" {
		args = append(args, filter.Description)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "description", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	result, err := stmt.Exec(args...)
	if err != nil {
		return errors.Wrap(err, "Delete profile")
	}

	n, err := result.RowsAffected()
	if err != nil {
		return errors.Wrap(err, "Fetch affected rows")
	}
	if n != 1 {
		return fmt.Errorf("Query deleted %d rows instead of 1", n)
	}

	return nil
}

// UpdateProfile updates the profile matching the given key parameters.
// generator: profile Update
func (c *ClusterTx) UpdateProfile(project string, name string, object Profile) error {
	id, err := c.GetProfileID(project, name)
	if err != nil {
		return errors.Wrap(err, "Get profile")
	}

	stmt := c.stmt(profileUpdate)
	result, err := stmt.Exec(object.Project, object.Name, object.Description, id)
	if err != nil {
		return errors.Wrap(err, "Update profile")
	}

	n, err := result.RowsAffected()
	if err != nil {
		return errors.Wrap(err, "Fetch affected rows")
	}
	if n != 1 {
		return fmt.Errorf("Query updated %d rows instead of 1", n)
	}

	// Delete current config.
	stmt = c.stmt(profileDeleteConfigRef)
	_, err = stmt.Exec(id)
	if err != nil {
		return errors.Wrap(err, "Delete current config")
	}

	// Insert config reference.
	stmt = c.stmt(profileCreateConfigRef)
	for key, value := range object.Config {
		if value == "" {
			continue
		}
		_, err := stmt.Exec(id, key, value)
		if err != nil {
			return errors.Wrap(err, "Insert config for profile")
		}
	}

	// Delete current devices.
	stmt = c.stmt(profileDeleteDevicesRef)
	_, err = stmt.Exec(id)
	if err != nil {
		return errors.Wrap(err, "Delete current devices")
	}

	// Insert devices reference.
	for name, config := range object.Devices {
		typ, ok := config["type"]
		if !ok {
			return fmt.Errorf("No type for device %s", name)
		}
		typCode, err := deviceTypeToInt(typ)
		if err != nil {
			return errors.Wrapf(err, "Device type code for %s", typ)
		}
		stmt = c.stmt(profileCreateDevicesRef)
		result, err := stmt.Exec(id, name, typCode)
		if err != nil {
			return errors.Wrapf(err, "Insert device %s", name)
		}
		deviceID, err := result.LastInsertId()
		if err != nil {
			return errors.Wrap(err, "Failed to fetch device ID")
		}
		stmt = c.stmt(profileCreateDevicesConfigRef)
		for key, value := range config {
			if value == "" {
				continue
			}
			_, err := stmt.Exec(deviceID, key, value)
			if err != nil {
				return errors.Wrap(err, "Insert config for profile")
			}
		}
	}

	return nil
}
