//go:build linux && cgo && !agent
// +build linux,cgo,!agent

package db

// The code below was generated by lxd-generate - DO NOT EDIT!

import (
	"fmt"
	"github.com/lxc/lxd/lxd/db/cluster"
	"github.com/lxc/lxd/lxd/db/query"
	"github.com/lxc/lxd/shared/api"
	"github.com/pkg/errors"
	"strings"
)

var _ = api.ServerEnvironment{}

var imageObjects = cluster.RegisterStmt(`
SELECT images.id, projects.name AS project, images.fingerprint, images.type, images.filename, images.size, images.public, images.architecture, images.creation_date, images.expiry_date, images.upload_date, images.cached, images.last_use_date, images.auto_update
  FROM images JOIN projects ON images.project_id = projects.id
  ORDER BY projects.id, images.fingerprint
`)

var imageObjectsByProject = cluster.RegisterStmt(`
SELECT images.id, projects.name AS project, images.fingerprint, images.type, images.filename, images.size, images.public, images.architecture, images.creation_date, images.expiry_date, images.upload_date, images.cached, images.last_use_date, images.auto_update
  FROM images JOIN projects ON images.project_id = projects.id
  WHERE project = ? ORDER BY projects.id, images.fingerprint
`)

// GetImages returns all available images.
// generator: image List
func (c *ClusterTx) GetImages(filter ImageFilter) ([]Image, error) {
	// Result slice.
	objects := make([]Image, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	stmt := c.stmt(imageObjects)
	args := []interface{}{}
	stmtStr := cluster.GetRegisteredStmt(imageObjects)
	if filter.Fingerprint != "" {
		args = append(args, filter.Fingerprint)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s LIKE ?%s", stmtBody, "fingerprint", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	if filter.Public != false {
		args = append(args, filter.Public)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "public", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	if filter.Cached != false {
		args = append(args, filter.Cached)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "cached", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	if filter.AutoUpdate != false {
		args = append(args, filter.AutoUpdate)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "auto_update", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, Image{})
		return []interface{}{
			&objects[i].ID,
			&objects[i].Project,
			&objects[i].Fingerprint,
			&objects[i].Type,
			&objects[i].Filename,
			&objects[i].Size,
			&objects[i].Public,
			&objects[i].Architecture,
			&objects[i].CreationDate,
			&objects[i].ExpiryDate,
			&objects[i].UploadDate,
			&objects[i].Cached,
			&objects[i].LastUseDate,
			&objects[i].AutoUpdate,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch images")
	}

	return objects, nil
}

// GetImagesByProject returns all available images.
// generator: image List-by-Project
func (c *ClusterTx) GetImagesByProject(project string, filter ImageFilter) ([]Image, error) {
	// Result slice.
	objects := make([]Image, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	stmt := c.stmt(imageObjectsByProject)
	args := []interface{}{
		project,
	}
	stmtStr := cluster.GetRegisteredStmt(imageObjectsByProject)
	if filter.Fingerprint != "" {
		args = append(args, filter.Fingerprint)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s LIKE ?%s", stmtBody, "fingerprint", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	if filter.Public != false {
		args = append(args, filter.Public)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "public", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	if filter.Cached != false {
		args = append(args, filter.Cached)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "cached", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	if filter.AutoUpdate != false {
		args = append(args, filter.AutoUpdate)
		stmtParts := strings.Split(stmtStr, "ORDER BY")
		stmtBody := stmtParts[0]
		stmtOrderBy := ""
		if len(stmtParts) == 2 {
			stmtOrderBy = " ORDER BY " + stmtParts[1]
		}
		if strings.Contains(stmtBody, "WHERE") {
			stmtBody += " AND "
		} else {
			stmtBody += " WHERE "
		}
		fullStmt := fmt.Sprintf("%s%s = ?%s", stmtBody, "auto_update", stmtOrderBy)
		stmt = c.stmt(cluster.RegisterStmtIfNew(fullStmt))
	}

	// Dest function for scanning a row.
	dest := func(i int) []interface{} {
		objects = append(objects, Image{})
		return []interface{}{
			&objects[i].ID,
			&objects[i].Project,
			&objects[i].Fingerprint,
			&objects[i].Type,
			&objects[i].Filename,
			&objects[i].Size,
			&objects[i].Public,
			&objects[i].Architecture,
			&objects[i].CreationDate,
			&objects[i].ExpiryDate,
			&objects[i].UploadDate,
			&objects[i].Cached,
			&objects[i].LastUseDate,
			&objects[i].AutoUpdate,
		}
	}

	// Select.
	err := query.SelectObjects(stmt, dest, args...)
	if err != nil {
		return nil, errors.Wrap(err, "Failed to fetch images")
	}

	return objects, nil
}
