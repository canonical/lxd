(cluster-link-config)=
# Cluster link configuration

Each cluster link has its own key/value configuration with the following supported namespaces:

- `user` (free form key/value for user metadata)

The following keys are currently supported:

% Include content from [../metadata.txt](../metadata.txt)
```{include} ../metadata.txt
    :start-after: <!-- config group cluster-link start -->
    :end-before: <!-- config group cluster-link end -->
```

## Related topics

{{clustering_how}}

{{clustering_exp}}
