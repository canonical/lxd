(ref-clusters)=
# Clusters

Cluster configuration consists of different categories:

## Cluster member configuration

Each cluster member has its own configuration.

```{toctree}
:titlesonly:
cluster_member_config
```

## Cluster link configuration

Each cluster link has its own configuration.

```{toctree}
:titlesonly:
cluster_link_config
```

## Additional information

Server configuration: The server configuration is shared by all cluster members. See {ref}`server` for available server configuration options.

## Related topics

{{clustering_how}}

{{clustering_exp}}
